{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Create Splits",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/etl/data_splitter.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}